<h1>Searching for - <%= @search_term %></h1>
<p>Powered By Food2Fork.com</p>

<% if (@recipes.nil? or @recipes == []) %>

    <p><h2>No recipes found with relevant information... Sorry</h2></p>

<% else %>
    <table border="1">
      <tr>
        <th>Image</th>
        <th>Title</th>
        <th>Social rank</th>
      </tr>

      <% @recipes.each do |recipe| %>
          <tr>
            <td><%= link_to(image_tag(recipe["image_url"],size: "100x100"), recipe['f2f_url'])%></td>
            <td><%= link_to(sanitize(recipe["title"]), recipe['f2f_url']) %></td>
            <td><%= sprintf '%.2f', recipe["social_rank"] %></td>




          </tr>
      <% end %>
    </table>
<% end %>